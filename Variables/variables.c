#include <stdio.h>

int main(){

    char name[] = "James";
    int age = 70;

    printf("There once was a man named %s,\n", name);
    printf("He was %d years old.\n", age);
    printf("He liked the name %s,\n", name);
    printf("But %s like being %d.\n", name, age);
}